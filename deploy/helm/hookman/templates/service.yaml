apiVersion: v1
kind: Service
metadata:
  name: {{ include "hookman.fullname" . }}
  labels:
    {{- include "hookman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hookman.selectorLabels" . | nindent 4 }}
